# frozen_string_literal: true

RSpec.describe Sb::Omniauth::Kakao do
  it "has a version number" do
    expect(Sb::Omniauth::Kakao::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
